
> 每次使用配置文件，我们都需要实例话一个操作配置文件的对象，如果在系统很多地方都需要使用配置文件的内容，那么配置文件的对象就会
不计其数，如果每个配置文件内容很少，占用内存也比较少，但是如果内容多，将会大大浪费我们的内存空间，因此，对于获取配置文件内容这种
类我们仅仅需要一个实例足矣，从而引出了我们的单例模式！
	 单例模式：在系统运行中，我们仅仅需要某个类的一个实例

## 定义
保证一个类只有一个实例，并提供一个访问它的全局访问点。